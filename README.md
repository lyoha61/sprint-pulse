# SprintPulse — Dev Team Dashboard

## Быстрый запуск
Поднять контейнеры:
```bash
npm run prod:up
```
Открыть проект (пока только фронт):
```bash
http://localhost:80/
```
## Описание
SprintPulse — прототип дашборда для тимлида разработки, который позволяет отслеживать ключевые метрики команды за спринты и быстро оценивать состояние разработки.

Цель — дать визуальное представление о производительности, качестве и стабильности работы команды без ручного сбора данных из разных систем.

## Функциональность

- Просмотр метрик команды за 3–4 спринта
- Переключение между спринтами
- Отображение ключевых KPI:
  - выполненные задачи
  - баги
  - velocity команды
  - время code review
  - процент задач в срок
  - частота релизов
- Подсветка проблемных метрик (status: OK / Warning / Critical)
- Интерактивные графики
- AI-инсайты по выбранному спринту (опционально)

Чтобы временно отключить генерацию через модель и использовать только fallback-аналитику:

```env
USE_LLM_INSIGHTS=false
```

## Технологии

### Frontend
- Vue 3 (Composition API)
- Vite
- Tailwind CSS
- Lucide (icons)

## AI-аналитика

Установка и настройка локальной AI-модели

```text
docker exec sprintpulse_ollama ollama pull llama3.2:3b
```

Проверка модели

```text
docker exec sprintpulse_ollama ollama list
```

В проекте реализована подготовка метрик для AI-аналитики.

Каждая метрика получает нормализованную оценку:

```text
AI-score: 1–100
```

Чем выше `AI-score`, тем лучше состояние метрики.

### Для чего нужен AI-score

У разных метрик разные единицы измерения:

- проценты;
- количество багов;
- количество итераций ревью;
- загрузка команды;
- выполнение спринта.

Чтобы AI было проще анализировать данные, сырые значения переводятся в единую шкалу от 1 до 100.


### Визуализация
- Chart.js / ECharts

### Данные
- Mock data (JSON / JS modules)
- Имитация API слоя (services layer)


## Архитектура

- Frontend-only приложение
- Слой сервисов для абстракции данных (подготовка под будущий backend)
- Mock API вместо реального сервера


## Критерии реализации

- Рабочий интерактивный прототип
- Реалистичные данные за несколько спринтов
- Понятные и интерпретируемые метрики
- Быстрое восприятие состояния команды на главном экране
- Минимум 1 интерактивный элемент (переключение спринтов / фильтр)
- Визуальная индикация проблемных зон


## Использование AI

- Генерация набора метрик
- Помощь в проектировании UI
- Генерация mock-данных
- Помощь в разработке компонентов и логики


## Статус

MVP прототип (учебный проект)
